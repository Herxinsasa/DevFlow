# <系统或服务名>接口契约

> 状态：草稿 / 待确认 / 已确认
> 需求规格：<路径>
> 需求设计：<路径>
> 提供方：<工程、服务或维护方>
> 消费方：<工程、客户端或维护方>
> 架构形态：B/S / C/S / 其他
> 通信方式：REST / RPC / 消息订阅 / 其他

字段表是契约依据，JSON 或消息示例只用于展示形态。示例必须与字段表一致；JSON 示例必须是合法 JSON，不使用行内注释解释字段。

## 1. 业务模块

用一段话说明模块职责、提供能力和消费关系，使未阅读需求与详细设计的读者也能理解接口用途。

| 服务编号 | 服务 Key | 路径或命名空间 | 中文名称 | 职责 |
|---|---|---|---|---|
| <编号；不适用则 —> | <Key> | <REST 根路径或 RPC 命名空间> | <名称> | <职责> |

### 1.1 通信约定

| 项目 | 约定 |
|---|---|
| 调用方向 | <消费方到提供方> |
| 接口标识 | <REST 方法与 URI，或 RPC 服务 ID、消息 ID 与 Key> |
| 序列化 | <JSON、强类型结构体或项目既有格式> |
| 通用包络 | <统一返回或消息外层；无则 —> |
| 兼容规则 | <字段新增、废弃、默认值和版本规则> |

## 2. 错误码

| 错误码 | 错误码 Key | 适用范围 | 说明 | 调用方处理 |
|---|---|---|---|---|
| <值> | <Key> | <接口、通知或模块> | <语义> | <重试、提示、忽略或终止> |

复用其他规范的错误码时只保留稳定路径和章节引用，不复制整张定义表。

## 3. 枚举定义

每个枚举使用独立三级标题，标题只写枚举中文名称或类型名称。

### <枚举名称>

| 枚举值 | 枚举名 | 说明 |
|---|---|---|
| <值> | <名称> | <业务或协议语义> |

复用其他文档的枚举时写明稳定文档路径和章节；本契约新增或修改的枚举必须完整列出。

## 4. 接口列表

| 接口标识 | 中文名称 | 调用方向 | 通信方式 | 说明 |
|---|---|---|---|---|
| <方法与 URI，或消息 ID 与 Key> | <名称> | <消费方到提供方> | REST / RPC | <用途> |

每个接口按以下固定结构展开。

### <接口中文名称>

> 关联业务：<业务模块或场景>
> 关联需求：REQ-001

#### 调用定义

REST 接口使用：

| 请求方法 | 请求 URI | URI 示例 | 调用方 | 权限或前置条件 |
|---|---|---|---|---|
| GET / POST / PUT / DELETE | <URI> | <完整示例> | <调用方> | <约束> |

RPC 接口使用：

| 服务 ID | 服务 Key | 消息 ID | 消息 Key | 调用方式 | 调用方 |
|---|---|---|---|---|---|
| <值> | <Key> | <值> | <Key> | 同步 / 异步 | <调用方> |

只保留与当前通信方式匹配的调用定义表。

#### 请求参数

| 字段 | 类型 | 必填 | 说明 | 默认值、范围或引用 |
|---|---|---|---|---|
| <字段或字段路径> | <协议或强类型定义> | 是 / 否 | <语义与单位> | <约束> |

没有请求包体时明确写“无请求包体”，不能省略本节。

#### 请求示例

使用与实际通信方式一致的 JSON、结构体或消息形态。示例不写字段注释，字段说明以请求参数表为准。

#### 返回参数

| 字段 | 类型 | 必返 | 说明 | 默认值、范围或引用 |
|---|---|---|---|---|
| <字段或字段路径> | <协议或强类型定义> | 是 / 否 | <语义与单位> | <约束> |

没有返回包体时明确写“无返回包体”，不能省略本节。

#### 返回示例

使用与实际通信方式一致的 JSON、结构体或消息形态。示例不写字段注释，字段说明以返回参数表为准。

#### 接口错误码

| 错误码 | 触发条件 | 调用方处理 |
|---|---|---|
| <引用模块错误码或专用错误码> | <确定条件> | <确定动作> |

## 5. 通知列表

| 通知 ID | 通知 Key | 中文名称 | 发送方 | 订阅方 | 说明 |
|---|---|---|---|---|---|
| <值> | <Key> | <名称> | <发送方> | <订阅方> | <用途> |

每个通知按以下固定结构展开；没有通知时保留本节并明确写“本次无通知”。

### <通知中文名称>

> 关联业务：<业务模块或场景>
> 关联需求：REQ-001

#### 通知定义

| 通知 ID | 通知 Key | 触发时机 | 发送方 | 订阅方 |
|---|---|---|---|---|
| <值> | <Key> | <确定条件> | <发送方> | <订阅方> |

#### 通知参数

| 字段 | 类型 | 必有 | 说明 | 默认值、范围或引用 |
|---|---|---|---|---|
| <字段或字段路径> | <协议或强类型定义> | 是 / 否 | <语义与单位> | <约束> |

#### 通知示例

使用与实际通信方式一致的合法 JSON、结构体或消息形态。字段说明以通知参数表为准。

## 6. 兼容与变更记录

| 版本 | 日期 | 变更类型 | 影响接口或通知 | 兼容策略 |
|---|---|---|---|---|
| <版本> | <日期> | 新增 / 修改 / 废弃 | <标识> | <升级、默认值或过渡规则> |
