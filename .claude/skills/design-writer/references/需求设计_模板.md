# <迭代ID> <需求主题>需求设计

> 状态：草稿 / 待确认 / 已确认
> 需求规格：<路径>
> 影响面：<路径>
> UI 原型：<路径；不涉及则 —>
> 接口契约：<路径；不涉及则 —>

## 设计导读

用一段话说明本次设计要解决的技术问题、整体实现路径和明确不改变的边界，不重复需求背景。

| 顺序 | 设计对象 | 层级或职责 | 关联需求 | 主要依赖 |
|---|---|---|---|---|
| 1 | <真实类型、组件或职责名称> | <公开层、数据层、适配器层、服务层、接口层等> | REQ-001 | <上游或前置对象> |

设计章节必须与上表一一对应，并按公开契约与配置、核心数据与状态、适配器或基础设施、管理器或服务层、对外接口或 UI 的依赖顺序排列。不存在的层不创建章节。

## 1. <真实设计对象名称>

> 所属层级：<层级或职责>
> 关联需求：REQ-001
> 关键位置：`<完整路径>:<符号>`
> 参考实现：`<路径>:<符号>`；无则省略

### 1.1 作用与边界

说明该对象在本次实现中的职责、输入输出边界、允许改变和必须保持不变的行为。

### 1.2 设计定义

按对象类型选择下列固定表格；不适用的表格不输出，不另建同级章节。

类型或结构体：

| 类型或字段 | 可见性 | 数据类型 | 语义 | 默认值、范围或兼容要求 |
|---|---|---|---|---|
| <名称> | 公开 / 内部 | <类型> | <含义> | <约束> |

类或组件：

| 成员或职责 | 输入 | 输出 | 协作对象 | 错误或状态规则 |
|---|---|---|---|---|
| <方法或职责> | <输入> | <输出> | <依赖> | <规则> |

数据映射：

| 来源 | 目标 | 转换规则 | 缺省或异常处理 |
|---|---|---|---|
| <来源字段或状态> | <目标字段或状态> | <确定规则> | <确定规则> |

只有存在真实方案选择、兼容取舍或模式偏离时，补充决策表：

| 决策 | 采用方案 | 关键依据 | 不采用方案 |
|---|---|---|---|
| DEC-001 | <方案> | <证据或约束> | <仅保留必要说明> |

### 1.3 协作与流程

仅在对象间存在调用顺序、状态变化、异步、回调或生命周期关系时保留。使用短步骤、时序图或状态表说明触发、处理、结果和失败路径，不重复字段定义。

### 1.4 实现约束

只记录本对象实际涉及的线程、并发、缓存、资源生命周期、性能、升级、回退、错误和兼容约束。

| 关键位置 | 必须实现 | 不变边界 |
|---|---|---|
| `<完整路径>:<符号>` | <明确要求> | <禁止改变的行为或契约> |

### 1.5 验证重点

- <需要验证的技术风险或边界；业务验收引用需求规格>

后续设计对象复制“作用与边界、设计定义、按需协作与流程、实现约束、验证重点”结构，编号连续，不改变信息槽含义。

## 跨对象协作

仅在单个对象章节无法清楚表达端到端调用、数据流、状态流或生命周期时保留。按真实依赖说明对象之间如何衔接，不重复各对象内部定义。

## 需求与验证追溯

| 需求项 | 设计章节 | 验证重点 | 接口契约章节 |
|---|---|---|---|
| REQ-001 | <章节标题> | <可验证结论> | <路径及章节；不涉及则 —> |

## 当前未决问题

仅保留影响编码的活动问题；无则省略。低置信度推断只能列在本节，确认前不得写入设计定义、实现约束或开发计划。

## 实现后回写

| 文档 | 待回写稳定事实 |
|---|---|
| <模块设计或架构总览> | <职责、接口、数据或约束> |

## 修订记录

仅确认后的重大技术变化保留；普通文字和排版修订不记录。

| 版本 | 日期 | 变更摘要 |
|---|---|---|
