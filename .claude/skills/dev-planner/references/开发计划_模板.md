# <迭代ID> <主题>开发计划

> 基线标识：<Git SHA / SVN revision / 文件快照标识>
> 初始工作区：<clean / dirty>
> 初始修改文件：<文件列表或 —>
> 需求规格：<路径>
> 影响面：<路径；不涉及独立文档则 —>
> 需求设计：<路径；不涉及则 —>
> 接口契约：<路径；不涉及则 —>

## 0. 文档边界

| 信息类型 | 主事实来源 | 本计划记录内容 |
|---|---|
| 业务目标和验收 | 需求规格 | 关联需求与覆盖状态 |
| 实现方式和代码边界 | 需求设计 | 任务的设计依据 |
| 跨端字段和接口 | 接口契约 | 任务或验证的引用 |
| 任务依赖和执行状态 | 本开发计划 | 任务、验证、依赖和阻塞 |
| 编码、日志和文档规范 | 项目约束 | 适用约束引用 |

计划不补写或改变需求、设计和接口契约；发现冲突时回到对应事实来源处理。

## 1. 执行状态

| 计划状态 | 当前执行项 | 下一执行项 | 活动阻塞 |
|---|---|---|---|
| 待执行 / 执行中 / 等待用户 / 阻塞 / 已完成 / 已取消 | TASK/VERIFY-ID 或 — | TASK/VERIFY-ID 或 — | BLOCK-ID 或 — |

本节只保存当前执行快照，不追加时间线或过程日志。按任务表顺序和前置任务执行；构建、审查、测试和用户验收只作为验证项执行。

## 2. 开发任务

| 任务ID | 任务目标 | 关联需求 | 设计依据 | 前置任务 | 状态 | 恢复信息 |
|---|---|---|---|---|---|---|
| TASK-001 | <动词开头的短交付目标> | REQ-001 / 公共支撑 | <需求设计或接口契约章节；T0/T1 可引用最小约束> | — | 待执行 | — |

表格行顺序是默认执行顺序，`前置任务` 是必须满足的任务依赖。任务目标不写结构体字段、枚举值、消息号、实现步骤或预计文件；完成、阻塞或跳过后，在恢复信息中简记实际修改文件、短结果、局部检查或恢复条件。

任务状态固定为：待执行 / 执行中 / 已完成 / 阻塞 / 跳过。

## 3. 验证计划

| 验证ID | 验证目标 | 关联任务 | 执行主体 | 状态 | 证据 |
|---|---|---|---|---|---|
| VERIFY-001 | <构建、审查、测试或验收目标> | TASK-001 / — | AI / 用户 / 外部方 | 待验证 | — |

验证状态固定为：待验证 / 已通过 / 未通过 / 无法执行 / 待用户验收。

## 4. 需求覆盖

| 需求ID | 开发任务 | 验证项 | 覆盖状态 |
|---|---|---|---|
| REQ-001 | TASK-001 | VERIFY-001 | 已覆盖 / 待补充 / 无需编码 |

每个本次范围内的需求项必须出现一次。无需编码时说明由哪个验证项覆盖，或在覆盖结论中写明原因。

## 5. 外部依赖

| 依赖ID | 依赖项 | 影响范围 | 就绪条件 | 状态 |
|---|---|---|---|---|
| DEP-001 | <外部团队、SDK、设备、环境、权限或协议> | <TASK/VERIFY ID> | <可判断条件> | 未就绪 / 已就绪 |

没有外部依赖时保留本节并写“无外部依赖”。任务之间的依赖只写在任务列表，不在本节重复。

## 6. 活动阻塞

| 阻塞ID | 阻塞事项 | 影响项 | 解除条件 | 责任方 |
|---|---|---|---|---|
| BLOCK-001 | <当前阻塞事实> | <TASK/VERIFY ID> | <可判断条件> | AI / 用户 / 外部方 |

没有活动阻塞时保留本节并写“无活动阻塞”。外部依赖只有在未就绪且实际阻止当前执行时才同步进入本节。
