# <迭代ID> <需求主题>

> 状态：草稿 / 待确认 / 已确认
> 来源：<用户输入或文档路径>
> 日期：YYYY-MM-DD

## 1. 基本信息

| 项 | 内容 |
|---|---|
| 目标 | <本次迭代要达到的结果> |
| 业务场景 | <谁在什么场景下触发或使用，以及要解决的问题> |
| 范围 | <本次包含的业务范围> |
| 非目标 | <明确不做的内容；无则省略本行> |

## 2. 需求项

| 需求ID | 名称与目标 | 触发与前置条件 | 正常行为与结果 | UI影响 |
|---|---|---|---|---|
| REQ-001 | <名称；要达到的业务结果> | <何时发生及必要前置条件> | <主要业务行为及可观察结果> | 是 / 否；<页面或 —> |

涉及 UI 时，在对应需求项下按需补充：显示位置、显示条件、隐藏条件、交互结果、单位、精度、无数据和旧数据行为。

实际涉及异常、依赖、兼容、数据口径或非目标时，在对应需求项下补充短结论；这些内容会改变行为或验收时不得省略。

当一行无法清楚表达且相关事实会改变行为、验收或后续影响分析范围时，才按以下结构补充；不重复表格内容，不为简单需求展开：

### REQ-001 <需求名称>

- 业务规则：<状态、时序、权限、数据口径或跨步骤规则；无则省略>。
- 外部边界：<实际涉及的用户、设备、外部系统或上下游业务；无则省略>。
- 异常与兼容：<失败、取消、历史数据、旧行为或回退要求；无则省略>。

该部分只描述已确认的业务事实和可观察结果，不写代码位置、类、字段结构、调用链、接口实现方式或任务拆分。

## 3. 需求初判

| 需求ID | 初判级别 |
|---|---|
| REQ-001 | T0 / T1 / T2 / T3 |

初判级别用于确定分析深度，最终级别以影响面分析结论为准。

## 4. 验收标准

| 验收ID | 需求ID | 前置条件 | 操作或输入 | 期望结果 |
|---|---|---|---|---|
| AC-001 | REQ-001 | <条件> | <操作> | <可观察结果> |

## 5. 简要影响结论

仅 T0/T1 保留；T2/T3 使用独立影响面文档。

| 需求ID | 级别 | 影响边界 | 变化概述 | 图谱状态 |
|---|---|---|---|---|
| REQ-001 | T0 / T1 | <模块或独立对象> | <已有行为变化；无则 —> | 已使用 / 不可用已降级 |

## 6. 当前阻塞问题

仅存在会改变行为、范围、UI、数据、错误处理、兼容性或验收结果的问题时保留。

| ID | 需求ID | 问题 | 需要用户确认 |
|---|---|---|---|
| Q-001 | REQ-001 | <单一明确问题> | <需要决定的内容> |

## 填写规则

- 表格只填表头定义的信息；每个单元格只表达一个语义。
- 表格写短结论，依据放在对应正文；状态列只写状态。
- 没有内容或变化统一写 `—`；可选章节整体无内容时省略。
- 每行必须独立可读，不写“同 REQ-001”；可明确写“复用 REQ-001 的接口”。
- 单元格只写一个短结论并保持一眼可读；需要解释依据时移到对应正文，不为控制长度机械删减必要事实。
- 不写 Agent、工具命令、分析过程、实现方案和后续流程标注。
- 业务场景说明谁在什么情况下需要本次变化，不写项目背景长文或方案理由。
- 每个需求项必须具备目标、触发与前置条件、正常行为与结果、验收标准、UI 影响和当前阻塞；当前阻塞统一放第 6 节。
